# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/
build/
dist/

# Jupyter / editor / OS
.ipynb_checkpoints/
.vscode/
.DS_Store

# real_finetune/ Colab outputs, if ever run locally
real_finetune/outputs/
real_finetune/*_lora/

# NOTE: data/*.csv is generated by generate_data.py but tiny + deterministic, so
# it's committed on purpose -- notebooks/app work the moment someone clones.
